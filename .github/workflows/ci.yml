name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.14

      - name: Install dependencies
        run: uv sync --frozen

      - name: Ruff check
        run: uv run ruff check --preview .

      - name: Ruff format check
        run: uv run ruff format --check .

  test:
    name: Pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.14

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run tests with coverage
        env:
          SECRET_KEY: a1a2c6c6a281655a9aa4bd918ae10362b4b5719a8ae4185438e6ff2161020845
          MAIL_FROM: "noreply@winterfell.local"
          MAIL_FROM_NAME: "Winterfell"
          MAIL_USERNAME: "noreply@winterfell.local"
          MAIL_PASSWORD: "password"
          MAIL_PORT: "587"
          MAIL_SERVER: "smtp.winterfell.local"
        run: >
          uv run pytest
          --cov=app
          --cov-report=term-missing
          --cov-report=xml
          --cov-fail-under=90
