[project]
name = "winterfell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi[standard]",
  "httpx2>=2.4.0",
  "pyjwt>=2.13.0",
  "pytest>=9.1.0",
  "pytest-cov>=7.1.0",
  "sqlmodel",
  "uvicorn[standard]",
]
