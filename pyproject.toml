[project]
name = "winterfell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi[standard]",
  "pyjwt>=2.13.0",
  "python-jwt>=4.1.0",
  "sqlmodel",
  "uvicorn[standard]",
]
